import pandas as pd

# Abuse indicator columns
abuse_cols = [
    'abandonment',
    'adoption_disruption_dissolution',
    'behavioral_health_concerns_child',
    'behavioral_health_concerns_parent_caregiver',
    'child_sexually_acting_out',
    'domestic_violence',
    'homelessness',
    'inadequate_basic_needs_clothing_food_hygiene',
    'inadequate_education',
    'inadequate_health_care',
    'inadequate_nurturing_affection',
    'inadequate_shelter_housing',
    'inappropriate_discipline',
    'isolation',
    'lack_of_caregiver',
    'lack_of_supervision',
    'other',
    'substance_use_by_child',
    'substance_use_by_parent_caregiver',
    'truancy'
]

results_list = []

for abuse in abuse_cols:

    df_temp = df.copy()

    # Force abuse category to be present
    df_temp[abuse] = 1

    # Predict probability
    probs = result.predict(df_temp)

    results_list.append({
        'subcategory': abuse,
        'avg_marginal_pred_prob': probs.mean()
    })

ampp_df = pd.DataFrame(results_list)

ampp_df['avg_marginal_pred_prob'] = (
    ampp_df['avg_marginal_pred_prob'] * 100
)

ampp_df = ampp_df.sort_values(
    'avg_marginal_pred_prob',
    ascending=False
)

print(ampp_df)

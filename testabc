import pandas as pd

results = []

for col in abuse_columns:

    X_temp = X.copy()

    # Set abuse category present for everyone
    X_temp[col] = 1

    probs = result.predict(X_temp)

    results.append({
        "subcategory": col,
        "predicted_probability": probs.mean()
    })

pp_df = pd.DataFrame(results)

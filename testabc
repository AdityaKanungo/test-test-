import pandas as pd

group_cols = ["long_person_id", "person_id"]

sizes = df.groupby(group_cols).size()

# total number of pairwise comparisons the algorithm will do
total_pairs = (sizes * (sizes - 1) // 2).sum()

print("Number of groups:", len(sizes))
print("Max group size:", sizes.max())
print("Total pairwise comparisons:", int(total_pairs))

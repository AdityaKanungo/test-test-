import matplotlib.pyplot as plt

# Make sure date columns are datetime
date_cols = [
    "gps_referral_received_date",
    "gps_incident_begin_date",
    "cps_referral_received_date",
    "cps_incident_begin_date"
]

for col in date_cols:
    final_df[col] = pd.to_datetime(final_df[col], errors="coerce")

# Calculate day differences
final_df["days_gps_ref_to_cps_ref"] = (
    final_df["cps_referral_received_date"] - final_df["gps_referral_received_date"]
).dt.days

final_df["days_gps_ref_to_cps_incident"] = (
    final_df["cps_incident_begin_date"] - final_df["gps_referral_received_date"]
).dt.days

final_df["days_gps_incident_to_cps_incident"] = (
    final_df["cps_incident_begin_date"] - final_df["gps_incident_begin_date"]
).dt.days

# Histograms
hist_vars = {
    "days_gps_ref_to_cps_ref": "GPS referral date to CPS referral date",
    "days_gps_ref_to_cps_incident": "GPS referral date to CPS incident start date",
    "days_gps_incident_to_cps_incident": "GPS incident start date to CPS incident start date"
}

for col, title in hist_vars.items():
    plt.figure(figsize=(9, 5))
    plt.hist(final_df[col].dropna(), bins=50)
    plt.axvline(0, linestyle="--")
    plt.title(title)
    plt.xlabel("Days from GPS to CPS")
    plt.ylabel("Number of children")
    plt.show()

Negative values will appear automatically if CPS happened before GPS.

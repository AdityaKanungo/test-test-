Got it — you want 3 validation checks on your df using cwis_created_date and referral_received_date. Below is clean, production-style pandas code 👇


---

✅ Step 0: Ensure datetime format

import pandas as pd

df['cwis_created_date'] = pd.to_datetime(df['cwis_created_date'], errors='coerce')
df['referral_received_date'] = pd.to_datetime(df['referral_received_date'], errors='coerce')


---

🔹 1. Count where cwis_created_date < referral_received_date

cond1 = df['cwis_created_date'] < df['referral_received_date']

count_1 = cond1.sum()

print("1. Count where cwis_created_date < referral_received_date:", count_1)


---

🔹 2. Among those, cwis_created_date > Jan 1, 2023 AND referral within range

(Assuming “within range” = ≤ Dec 31, 2025 — adjust if needed)

start_date = pd.Timestamp('2023-01-01')
end_date = pd.Timestamp('2025-12-31')

cond2 = (
    cond1 &
    (df['cwis_created_date'] > start_date) &
    (df['referral_received_date'] <= end_date)
)

count_2 = cond2.sum()

print("2. Count where created > Jan 1 2023 & referral within range:", count_2)


---

🔹 3. Referral > Dec 31, 2025 BUT created <= Dec 31, 2025

cutoff = pd.Timestamp('2025-12-31')

cond3 = (
    (df['referral_received_date'] > cutoff) &
    (df['cwis_created_date'] <= cutoff)
)

count_3 = cond3.sum()

print("3. Count where referral > 2025 but created within range:", count_3)


---

🔥 Bonus (Highly recommended for your analysis)

If you want actual records for debugging / audit:

df_check1 = df[cond1]
df_check2 = df[cond2]
df_check3 = df[cond3]


---

⚠️ Notes (important for your CWIS dataset)

errors='coerce' handles bad dates → converts to NaT

Comparisons automatically ignore NaT

If you want to explicitly track missing issues, add:


df[df['cwis_created_date'].isna() | df['referral_received_date'].isna()]


---

If you want, I can:

Add referral-level aggregation (dedupe by referral_id)

Or create a QA summary table for your PPT (counts + %)

Or flag these as data quality indicators (DQ flags) for your pipeline


Just tell me 👍
